trigger:
- develop

stages:
- stage: BuildAndPushStage
  displayName: 'Build and Push Docker Image'
  
  jobs:
  - template: doc-build.yaml
    parameters:
      imageName: 'mydockerhubusername/spc-image'
      dockerfilePath: '$(Build.SourcesDirectory)/Dockerfile'
      tags: '1.0'






